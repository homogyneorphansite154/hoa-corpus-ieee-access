# Analysis pipeline dependencies (Python 3.9+)
numpy>=1.24
scipy>=1.11
soundfile>=0.12
matplotlib>=3.7
pyyaml>=6.0

# Optional, Fig. 4 only: basemap tiles for the geographic map.
# Without these the other figures still render; Fig. 4 is skipped and the
# published version under figures/ is used instead.
# contextily>=1.3
# geopandas>=0.13
